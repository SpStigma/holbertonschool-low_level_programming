#include "main.h"
#include <stdio.h>

/**
 * main - afficher tjr le nom du fichier
 * @argc: le nombre d argument
 * @argv: l'affichafge des arguments
 *
 * Return: le nom du fichie
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
