#include <stdio.h>

/**
 * main - alpha knsmljvmdqfYGJJDITU
 * Return: to sucess
 */

int main(void)
{
	int lower;
	int upper;

	for (lower = 'a' ; lower <= 'z' ; lower++)
{
	putchar(lower);
}
for (upper = 'A' ; upper <= 'Z' ; upper++)
{
	putchar(upper);
}
	printf("\n");
return (0);
	}

