#include "main.h"

/**
 * _strlen - affiche le nombre de charater ds une chaine
 * @s: string
 *
 * Return: to sucess
 */

int _strlen(char *s)
{
	int i = '0';

	for (i = '0' ; s[i] != 0 ; i++)
	{
	}
	return(i);
}
