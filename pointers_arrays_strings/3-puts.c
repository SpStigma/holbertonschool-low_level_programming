#include "main.h"

/**
 * _puts - affiche la phrase
 * @str: la chaine de caractère a imprimer
 *
 *Return: charactère
 */

void _puts(char *str)
{
	char c;

	for (str ; str[c] != 0 ; c++)
	{
	}
	return (c);
	_putchar('\n');
}
